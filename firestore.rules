rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Unrestricted access for any authenticated user.
    // This is for demo purposes to prevent any permission-denied errors.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
